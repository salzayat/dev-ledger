# Tasks: Add Default Cost Class

- [x] 1.1 ~1 `packages/capture`: `default` in the classes file, validated; `class set --default`.
- [x] 1.2 ~2 `packages/flow`: carry the default; resolve session, trailer, spec, default, else unclassified;
      source `default` on the class panel; panel wording.
- [x] 1.3 ~1 This repository declares `rd` as its default; docs.
- [x] 2.1 ~1 Tests: a spec declaration wins over the default, a record citing no spec takes it, a default
      outside the vocabulary is refused.
- [x] 2.2 ~1 `npm run check`.

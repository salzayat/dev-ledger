# Tasks: Add Release Cost Class

- [x] 1.1 ~1 `packages/capture`: `release` in the classes file, validated; `class set --release`.
- [x] 1.2 ~2 `packages/flow`: resolve session, trailer, spec, release rule, default; classify sessions on
      unmerged pull requests; sources `released` and `unreleased`; panel wording.
- [x] 1.3 ~1 This repository declares the rule and drops the blanket spec list and default; docs.
- [x] 2.1 ~1 Tests: a tagged change is production, an untagged merge and an unmerged pull request are R&D,
      a spec declaration wins; a rule outside the vocabulary is refused.
- [x] 2.2 ~1 `npm run check`.

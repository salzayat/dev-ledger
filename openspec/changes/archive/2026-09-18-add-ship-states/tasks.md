# Tasks: Add Ship States

- [x] 1.1 ~3 `packages/flow`: survival by blame at each release tag; ship state per change and unmerged pull
      request; spend, hours, and releases per state.
- [x] 1.2 ~1 Release rule `{ shipped, discarded }` with a `pending` row, in capture and flow; this
      repository's rule.
- [x] 1.3 ~2 Economics › Shipped sub-view and the headline sentence.
- [x] 1.4 ~1 Docs: methodology, contract, README.
- [x] 2.1 ~1 Tests: an overwritten attempt is discarded, a closed pull request is discarded, untagged work
      and an open pull request are pending, the rule classifies each, the sub-view renders.
- [x] 2.2 ~1 `npm run check`.

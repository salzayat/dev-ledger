# Tasks: Fix Agent Run Seconds

- [x] 1.1 ~1 `computeAgentRunSeconds` in `packages/capture`; `session end --transcript` fills it when the
      payload omits it.
- [x] 1.2 ~1 Docs: contract.
- [x] 2.1 ~1 Tests: one prompt, final turn, work under way, capped wait, no agent record; the command end to
      end.
- [x] 2.2 ~1 `npm run check`.
